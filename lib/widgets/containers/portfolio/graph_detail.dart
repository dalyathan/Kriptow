import 'package:flutter/material.dart';

class GraphDetail extends StatelessWidget {
  const GraphDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
