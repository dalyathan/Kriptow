import 'package:flutter/material.dart';

class MarketIcon extends StatelessWidget {
  const MarketIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
